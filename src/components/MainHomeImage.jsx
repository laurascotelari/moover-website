import { homeImage } from "../assets";

export const MainHomeImage = () => {
  return (
    <div key="" className={`w-auto h-auto relative`}>
      <img src={homeImage} alt={"placeholder"} className="w-screen" />
    </div>
  );
};
